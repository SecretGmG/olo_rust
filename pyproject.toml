[build-system]
requires = ["maturin>=1.2"]
build-backend = "maturin"

[project]
name = "olo_rust"
version = "0.1.0"
description = "OneLOop scalar integrals with Rust and Python bindings"
readme = "README.md"
requires-python = ">=3.8"